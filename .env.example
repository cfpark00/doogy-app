# Supabase Configuration
# Get these from https://app.supabase.com/project/YOUR_PROJECT/settings/api

# Public URL - safe to use in client apps
SUPABASE_URL=https://your-project-id.supabase.co

# Public anon key - safe to use in client apps (protected by RLS)
# This is a JWT token that starts with eyJ...
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...your-anon-key-here

# Service role key - NEVER use in client apps, only for backend/admin scripts
# ⚠️ DANGER: This bypasses RLS and has full database access
# Only use in server-side code or build scripts
# Get from: Settings > API > service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...your-service-role-key-here

# Backend API Configuration
# For local development, use http://localhost:3001
# For production, use your deployed backend URL
#BACKEND_URL=http://localhost:3001
BACKEND_URL=https://your-backend-url.run.app

# Google OAuth Configuration
# Get these from https://console.cloud.google.com/apis/credentials

# iOS OAuth 2.0 Client ID (safe to expose, used in iOS app)
GOOGLE_IOS_CLIENT_ID=your-project-id.apps.googleusercontent.com

# Web OAuth 2.0 Client ID (safe to expose, used for web auth)
GOOGLE_WEB_CLIENT_ID=your-project-id.apps.googleusercontent.com

# Web Client Secret (KEEP SECRET - only use server-side)
# ⚠️ Never expose this in client-side code
GOOGLE_WEB_CLIENT_SECRET=GOCSPX-your-client-secret-here